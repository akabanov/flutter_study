import 'package:flutter/material.dart';
import 'package:gor/route/shell_route.dart';

void main() {
  runApp(ShellRouteApp());
  // runApp(SRApp());
  // runApp(ARApp());
  // runApp(IWApp());
  // runApp(OEApp());
}
